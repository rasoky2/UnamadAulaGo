import 'package:flutter/material.dart';

class InformacionTab extends StatelessWidget {
  const InformacionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Información del Curso (Próximamente)'),
    );
  }
} 